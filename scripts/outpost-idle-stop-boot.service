[Unit]
Description=Reset outpost idle-stop activity on boot
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'mkdir -p /var/lib/outpost && date +%%s > /var/lib/outpost/last-activity'

[Install]
WantedBy=multi-user.target